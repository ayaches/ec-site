<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ecsite.mapper.CartMapper">

    <select id="findByUserId" resultType="com.example.ecsite.model.Cart">
        SELECT * FROM carts WHERE user_id = #{userId}
    </select>

    <insert id="insertCart" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO carts (user_id) VALUES (#{userId})
    </insert>

    <select id="findItemsByCartId" resultType="com.example.ecsite.model.CartItem">
        SELECT * FROM cart_items WHERE cart_id = #{cartId}
    </select>

    <select id="findItemByCartIdAndProductId" resultType="com.example.ecsite.model.CartItem">
        SELECT * FROM cart_items
        WHERE cart_id = #{cartId} AND product_id = #{productId}
    </select>

    <insert id="insertItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart_items (cart_id, product_id, quantity)
        VALUES (#{cartId}, #{productId}, #{quantity})
    </insert>

    <update id="updateItemQuantity">
        UPDATE cart_items SET quantity = #{quantity} WHERE id = #{id}
    </update>

    <delete id="deleteItemById">
        DELETE FROM cart_items WHERE id = #{id}
    </delete>

    <delete id="deleteItemsByCartId">
        DELETE FROM cart_items WHERE cart_id = #{cartId}
    </delete>

</mapper>