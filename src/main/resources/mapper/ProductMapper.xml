<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ecsite.mapper.ProductMapper">

    <select id="findAll" resultType="com.example.ecsite.model.Product">
        SELECT * FROM products ORDER BY id
    </select>

    <select id="findById" resultType="com.example.ecsite.model.Product">
        SELECT * FROM products WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO products (name, description, price, stock, image_url)
        VALUES (#{name}, #{description}, #{price}, #{stock}, #{imageUrl})
    </insert>

    <update id="update">
        UPDATE products
        SET name        = #{name},
        description = #{description},
        price       = #{price},
        stock       = #{stock},
        image_url   = #{imageUrl}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM products WHERE id = #{id}
    </delete>

</mapper>